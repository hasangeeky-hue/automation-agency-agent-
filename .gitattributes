# Shell scripts MUST keep LF. A CRLF checkout makes bash fail with an opaque
# "\r: command not found" on the VPS — the backup would silently never run.
*.sh text eol=lf
# The engine writes its own files with newline="\n"; keep the repo consistent.
*.py text eol=lf
